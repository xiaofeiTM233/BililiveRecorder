<UserControl x:Class="BililiveRecorder.WPF.Controls.TaskbarIconControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.openxmlformats.org/expression/blend/2008"
             xmlns:local="clr-namespace:BililiveRecorder.WPF.Controls"
             xmlns:models="clr-namespace:BililiveRecorder.WPF.Models"
             xmlns:l="clr-namespace:BililiveRecorder.WPF"
             xmlns:converters="clr-namespace:BililiveRecorder.WPF.Converters"
             mc:Ignorable="d"
             Visibility="{Binding Recorder, Converter={StaticResource IsNullToVisibilityCollapsedConverter}}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <!-- 鼠标悬停托盘图标时显示的富 tooltip，用 WPF Popup 自绘，避免第三方库把气泡弹到屏幕左上角 -->
        <Popup x:Key="TrayToolTipPopup"
               Placement="Absolute"
               AllowsTransparency="True"
               StaysOpen="False"
               PopupAnimation="Fade">
            <Border Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}"
                    BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="12"
                    MaxWidth="320"
                    Effect="{DynamicResource Shadow19}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"
                               FontWeight="SemiBold" FontSize="14"
                               Text="{Binding Recorder.WorkDirectory, StringFormat='工作目录：{0}'}" />
                    <TextBlock Grid.Row="1" Margin="0,4,0,0"
                               Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                               FontSize="12"
                               Text="{Binding Recorder.Rooms.Count, StringFormat='房间数：{0}'}" />
                    <ItemsControl Grid.Row="2" Margin="0,4,0,0"
                                  ItemsSource="{Binding Recorder.Rooms}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="16" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Ellipse Grid.Column="0" Width="8" Height="8" VerticalAlignment="Center"
                                             Fill="{Binding IsRecording, Converter={StaticResource BooleanToLiveStatusColorBrushConverter}}" />
                                    <TextBlock Grid.Column="1" VerticalAlignment="Center" Margin="4,0,0,0"
                                               Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"
                                               Text="{Binding Name}" />
                                    <TextBlock Grid.Column="2" VerticalAlignment="Center" Margin="8,0,0,0"
                                               Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                                               Text="{Binding StreamTitle}" />
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </Border>
        </Popup>

        <ContextMenu x:Key="TrayContextMenu">
            <MenuItem x:Name="menuItemShowHide" Header="显示/隐藏" Click="MenuItemShowHide_Click" />
            <MenuItem x:Name="menuItemStartAll" Header="开始全部录制" Click="MenuItemStartAll_Click" />
            <MenuItem x:Name="menuItemStopAll" Header="停止全部录制" Click="MenuItemStopAll_Click" />
            <MenuItem x:Name="menuItemOpenWorkDir" Header="打开工作目录" Click="MenuItemOpenWorkDir_Click" />
            <Separator />
            <MenuItem x:Name="menuItemExit" Header="退出" Click="MenuItemExit_Click" />
        </ContextMenu>
    </UserControl.Resources>

    <!-- 这个 UserControl 本身不可见，仅作为 NotifyIcon 的承载与生命周期管理 -->
    <Grid />
</UserControl>
